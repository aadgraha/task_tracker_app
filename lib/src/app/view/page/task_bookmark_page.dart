import 'package:flutter/widgets.dart';

class TaskBookmarkPage extends StatelessWidget {
  const TaskBookmarkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}